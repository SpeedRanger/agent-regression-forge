# Product Hunt Submit Handoff

> Date: 2026-06-08
> Product: Agent Regression Forge
> Status: ready for interactive Product Hunt submission

## Manual Blocker

Product Hunt submission requires an interactive logged-in Product Hunt account.
Do not claim a Product Hunt listing or rank until the public listing URL and
leaderboard proof are recorded in `launch-status.json` and `launch-ledger.md`.

## Submit Fields

Product name:

```text
Agent Regression Forge
```

Tagline:

```text
Turn AI agent failures into regression tests
```

Website:

```text
https://speedranger.github.io/agent-regression-forge/
```

Repository:

```text
https://github.com/SpeedRanger/agent-regression-forge
```

Description:

```text
Agent Regression Forge turns a failed AI coding-agent run into deterministic
regression checks, guardrails, a proof checklist, and the next prompt. Paste a
public/redacted transcript and observed breakage; get the prevention artifact
the next agent or reviewer must obey.
```

Topics:

```text
Artificial Intelligence, Developer Tools, GitHub, Open Source, Software Engineering, QA
```

First maker comment:

```text
AI coding agents are now good enough that the failure mode has changed.

The problem is not only "the agent made a mistake." The problem is a run that
looks done, touches the wrong surface, skips tests, adds a silent fallback, and
leaves no regression check behind.

Agent Regression Forge is a small proof layer after a failed run. Paste a public
or redacted transcript, intended behavior, and observed breakage. It writes a
regression pack: failure modes, required checks, test scaffolds, guardrails, and
the next prompt.

The first paid wedge is five $19 public/redacted failure-to-regression-pack
slots. The first $10 revenue target counts only with receipt/export/invoice
evidence.
```
